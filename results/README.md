# Results 文件夹结构说明

本文件夹存放所有训练和评估结果，按类别组织。

## 📁 文件夹结构

```
results/
├── README.md                    # 本说明文件
├── labels.csv                   # 地标点标签数据
├── valid_projects.txt           # 有效项目列表
├── training_histories/          # 训练历史记录
│   ├── training_history_*.json
│   └── ...
├── test_evaluations/            # 测试集评估结果
│   ├── test_*.json
│   ├── kfold_test_evaluation_results.json
│   └── ...
├── model_comparisons/           # 模型对比结果
│   ├── *comparison*.json
│   ├── *comparison*.csv
│   └── ...
├── figures/                     # 可视化图表
│   ├── *.png
│   └── ...
└── archived/                    # 归档的旧版本结果
    ├── v1/
    ├── v2/
    └── ...
```

## 📊 各文件夹说明

### training_histories/
存放所有训练历史记录，包括：
- `training_history_full.json` - 单次训练完整记录
- `training_history_full_aug_fps.json` - 增强+FPS训练记录
- `training_history_kfold.json` - K折交叉验证记录
- `training_history_kfold_aug_fps.json` - K折+增强+FPS记录
- `training_history_v2.json` - V2版本训练记录

### test_evaluations/
存放测试集评估结果，包括：
- `test_comparison_*.json` - 各种模型对比测试结果
- `kfold_test_evaluation_results.json` - K折模型测试评估
- `best_fold_model_evaluation_results.json` - 最佳折模型评估
- `single_model_on_kfold_testset_results.json` - 单模型K折测试集评估

### model_comparisons/
存放模型性能对比结果，包括：
- `error_report_comparison.csv` - 误差报告对比
- 各种模型对比的JSON文件

### figures/
存放可视化图表，包括：
- 训练曲线图
- 模型对比图
- 误差分析图

### archived/
归档旧版本的实验结果：
- `v1/` - 第一版实验结果
- `v2/` - 第二版实验结果

## 🔍 Git 管理说明

- 核心数据文件（`labels.csv`, `valid_projects.txt`）会提交到 Git
- 结果文件（JSON, CSV, PNG）通过 `.gitignore` 排除，避免仓库过大
- `README.md` 会提交，保持文件夹结构说明

## 📝 注意事项

1. 所有训练脚本会自动将结果保存到对应文件夹
2. 旧版本结果移动到 `archived/` 避免混淆
3. 系统文件（如 `Thumbs.db`）已清理并加入忽略列表
